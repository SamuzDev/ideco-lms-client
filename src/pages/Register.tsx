import { SignUpForm } from "@/components/auth/SignUpForm";

const Register = () => {
  return (
    <div className="bg-gray-900 min-h-screen w-full login-gradient flex items-center justify-center p-4">
      <SignUpForm />
    </div>
  );
};

export default Register;
